{
  "name": "prettier-plugin-angular-module-import-metadata",
  "version": "0.0.1",
  "description": "prettier plugin to sort angular imports",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --check \"src/**/*.ts\"",
    "example": "yarn build && prettier --plugin . test/example.ts",
    "test": "jest"
  },
  "dependencies": {
    "prettier": "2.8.3"
  },
  "devDependencies": {
    "@types/prettier": "2.7.2",
    "typescript": "^4.9.4"
  },
  "prettier": "@zeenea/prettier-config"
}
